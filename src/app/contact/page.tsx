import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us",
  description:
    "Reach out to PrimeCare Clinic via our dedicated channels. Our support team is available for medical coordination and inquiries.",
};

export default function ContactPage() {
  return <ContactClient />;
}
